# Contributing

Run `npm test` and `npm run build` before submitting a change. Keep institution-specific behavior in configuration where possible. New hard-coded publisher URL patterns need a public example DOI and a regression test.

Provider contributions must follow [docs/providers.md](docs/providers.md) and must not contain credentials or session data.
